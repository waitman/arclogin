
arclogin

This program logs into the Archiver.Photo repository server and fetches the 
authentication token (cookie). The cookie is stored in your home directory
in the file ~/.archiver.photo/cookie

Usage:

arclogin [username] [password]

Note: if you omit the [username] and [password] parameters, the program will
prompt you for input.
 


